<% if action_name == "edit" && controller_name == "libraries" %>
  <%= render "upload_edit" %>
<% else %>
  <%= render "upload" %>
<% end %>

<%= simple_nested_form_for(@library, url: (@library.new_record? ? libraries_path : library_path(@library)), html: {id: 'video_library_info'}) do |lib| %>
  <div class="exer_rht_col lib_form_rgt">
    <div style="color:red;">
      <% if @library.errors.full_messages.any? %>
      <ul>
      <% @library.errors.full_messages.each do |error_message| %>
        <li><%= error_message %></li>
      <% end %>
        </ul>
      <% end %>
    </div>
    <%= hidden_field_tag :video, "#{@video_id.present? ? @video_id : ''}", class: "video_id" %>
    <%= hidden_field_tag :image, "", class: "image_url" %>
    <div class="exr_field_col">
      <label>Title</label>
      <%= lib.text_field :title %>
    </div>
        
    <div class="exr_field_col">
      <label>Directions</label>
      <%= lib.text_area :directions %>
    </div>
      
    <div class="exr_field_col">
      <label>Category</label>
      <div class="fancy_select">
        <%= lib.select :category, Library::CATEGORIES, prompt: "Choose one" %>
      </div>
    </div>

    <div class="exr_field_col">
    	<label>Level of Difficulty</label>
      <div class="fancy_select">
        <%= lib.select :difficulty, Library::DIFFICULTIES, prompt: "Choose one" %>
      </div>
    </div>
      
    <!-- <div class="exr_field_col new_muscle_group">
      <h4>Additional Details</h4>
    	<label>Target Muscle Groups</label>
      <%#(1..5).each do |no|%>
        <input name="" type="button" class="fancy_input <%#=no == 1 ? 'active' : ''%>" value="<%#=no%>"/>
      <%#end%>
    </div>
    
    <span class="exr_field_col targets_sel">
      <%# i = 0 %>
      <%#= lib.fields_for :target_muscle_groups do |builder| %>
        <span class="edit_tmg" style="display:<%#=i > 0 ? 'none' : 'block'%>">
          <%# abc = TargetMuscleGroup::MUSCLES_TYPE[i][0] %>
          <%#=render partial: "target_muscle_group_fields", locals: {abc: abc, lib: builder, i: i} %>
          <%#i += 1%>
        </span>
      <%#end%>
    </span> -->
  </div>
<% end %>






// $('.drag_img').remove();
if($('.block_hide').length){
	$('.block_hide').parent().before("<%= escape_javascript(render(:partial => 'sub_blocks')) %>");
}
else{
	$('.drag_lib').append("<%= escape_javascript(render(:partial => 'sub_blocks')) %>");
}
$.fancybox.close();

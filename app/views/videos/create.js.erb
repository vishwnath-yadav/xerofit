<%if !@video.errors.present?%>
	$(".video_id").val("<%=@video.id%>");
	var progressbar = $( "#progressbar" );
	progressbar.progressbar( "value", 100 );
	$('#panda_video_enable').attr('disabled', false);
<%end%>
